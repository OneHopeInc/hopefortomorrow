/**
 * Tailwind CSS Configuration
 */
if (typeof tailwind !== 'undefined') {
	tailwind.config = {
		theme: {
			extend: {
				fontFamily: {
					sans: ['Inter Tight', 'sans-serif'],
				},
			},
		},
	};
}

